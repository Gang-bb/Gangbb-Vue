<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gangbb.core.mapper.SysLoginInfoMapper">
    <resultMap type="com.gangbb.core.model.entity.SysLoginInfo" id="SysLoginInfoResult">
		<id     property="infoId"        column="info_id"           />
		<result property="userName"      column="user_name"         />
		<result property="status"        column="status"            />
		<result property="ipAddress"        column="ip_address"            />
		<result property="loginLocation" column="login_location"    />
		<result property="browser"       column="browser"           />
		<result property="os"            column="os"                />
		<result property="msg"           column="msg"               />
		<result property="loginTime"     column="login_time"        />
	</resultMap>

	<insert id="insertLoginInfo" parameterType="com.gangbb.core.model.entity.SysLoginInfo">
		insert into sys_logininfor (user_name, status, ip_address, login_location, browser, os, msg, login_time)
		values (#{userName}, #{status}, #{ipAddress}, #{loginLocation}, #{browser}, #{os}, #{msg}, sysdate())
	</insert>

	<select id="selectLoginInfoList" parameterType="com.gangbb.core.model.entity.SysLoginInfo" resultMap="SysLoginInfoResult">
		select info_id, user_name, ip_address, login_location, browser, os, status, msg, login_time from sys_logininfor
		<where>
			<if test="ipAddress != null and ipAddress != ''">
				AND ipaddr like concat('%', #{ipAddress}, '%')
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
			<if test="userName != null and userName != ''">
				AND user_name like concat('%', #{userName}, '%')
			</if>
			<if test="params.beginTime != null and params.beginTime != ''"><!-- 开始时间检索 -->
				and date_format(login_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
			</if>
			<if test="params.endTime != null and params.endTime != ''"><!-- 结束时间检索 -->
				and date_format(login_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
			</if>
		</where>
		order by info_id desc
	</select>

	<delete id="deleteLoginInfoByIds" parameterType="Long">
 		delete from sys_logininfor where info_id in
 		<foreach collection="array" item="infoId" open="(" separator="," close=")">
 			#{infoId}
        </foreach>
 	</delete>

    <update id="cleanLoginInfo">
        truncate table sys_logininfor
    </update>

</mapper>